# Dependencias del pipeline LSA (cámara → clasificador → glosas → LLM).
# El motor ILSA (run_backend.py / ILSA.exe) corre clasificador y traductor en CPU.
#
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# Python 3.10–3.12 (opcional, binding llama.cpp CPU):
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
#
# Python 3.14: no instales llama-cpp-python; el backend usa llama-server.exe CPU.

# --- Visión / captura (app de escritorio) ---
opencv-python==4.11.0.86
mediapipe==0.10.21
numpy==1.26.4

# --- Clasificador de señas ---
torch==2.6.0

# --- Traductor semántico (GGUF Llama 3.2 1B, CPU) ---

# --- Síntesis de voz (escritorio) ---
pyttsx3==2.99

# --- Backend local para la extensión Chrome ---
fastapi==0.115.12
uvicorn==0.32.1
